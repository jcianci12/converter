import { NpmconfigclipboardstringPipe } from './npmconfigclipboardstring.pipe';

describe('NpmconfigclipboardstringPipe', () => {
  it('create an instance', () => {
    const pipe = new NpmconfigclipboardstringPipe();
    expect(pipe).toBeTruthy();
  });
});
