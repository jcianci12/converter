import { NpmconfigstringPipe } from './npmconfigstring.pipe';

describe('NpmconfigstringPipe', () => {
  it('create an instance', () => {
    const pipe = new NpmconfigstringPipe();
    expect(pipe).toBeTruthy();
  });
});
