import { JsonheadersPipe } from './jsonheaders.pipe';

describe('JsonheadersPipe', () => {
  it('create an instance', () => {
    const pipe = new JsonheadersPipe();
    expect(pipe).toBeTruthy();
  });
});
